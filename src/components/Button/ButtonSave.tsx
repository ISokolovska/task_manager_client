import React from "react";
import { Button } from "@mui/material";

const ButtonSave = () => {
  return (
    <div>
      <Button
        variant="outlined"
        // onClick={() => {
        //   alert("clicked");
        // }}
      >
        save
      </Button>
    </div>
  );
};

export default ButtonSave;
