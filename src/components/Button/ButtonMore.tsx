import React from "react";
import { Button } from "@mui/material";

const ButtonMore = () => {
  return (
    <div>
      <Button
        variant="outlined"
        // onClick={() => {
        //   alert("clicked");
        // }}
      >
        more
      </Button>
    </div>
  );
};

export default ButtonMore;
