import React from "react";
import { Button } from "@mui/material";

const ButtonYes = () => {
  return (
    <div>
      <Button
        variant="outlined"
        // onClick={() => {
        //   alert("clicked");
        // }}
      >
        yes
      </Button>
    </div>
  );
};

export default ButtonYes;
