import React from "react";
import { Button } from "@mui/material";

const ButtonAdd = () => {
  return (
    <div>
      <Button
        variant="outlined"
        // onClick={() => {
        //   alert("clicked");
        // }}
      >
        Add
      </Button>
    </div>
  );
};

export default ButtonAdd;
