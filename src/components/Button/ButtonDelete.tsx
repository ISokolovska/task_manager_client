import React from "react";
import { Button } from "@mui/material";

const ButtonDelete = () => {
  return (
    <div>
      <Button
        variant="outlined"
        // onClick={() => {
        //   alert("clicked");
        // }}
      >
        Delete
      </Button>
    </div>
  );
};

export default ButtonDelete;
