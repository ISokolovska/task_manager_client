import React from "react";
import { Button } from "@mui/material";

const ButtonActions = () => {
  return (
    <div>
      <Button
        variant="outlined"
        // onClick={() => {
        //   alert("clicked");
        // }}
      >
        actions
      </Button>
    </div>
  );
};

export default ButtonActions;
