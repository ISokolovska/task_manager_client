import React from "react";
import { Button } from "@mui/material";

const ButtonNo = () => {
  return (
    <div>
      <Button
        variant="outlined"
        // onClick={() => {
        //   alert("clicked");
        // }}
      >
        No
      </Button>
    </div>
  );
};

export default ButtonNo;
