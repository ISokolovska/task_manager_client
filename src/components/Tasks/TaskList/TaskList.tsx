import React from "react";

const TaskList = () => {
  return (
    <div>
      <ul>
        {/* {getTopAnime.data.map((task) => (<li task={task} key={task.id} />))} */}
      </ul>
    </div>
  );
};

export default TaskList;
